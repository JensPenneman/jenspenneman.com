/** The dash between the two ends of a date range: an en dash, the typographic
 * range dash in every language this CV speaks, glued to both ends with
 * no-break spaces so a period never wraps around it. */
export const RANGE_DASH = "\u00a0\u2013\u00a0";
